# MorphFlow does not accept API keys through environment files.
# Configure secrets in the local Settings page; they are stored in macOS Keychain.

# Required non-secret path. It must be absolute and outside the source repository.
# Recommended on macOS: /Users/<you>/Library/Application Support/MorphFlow
MORPHFLOW_DATA_DIR=
